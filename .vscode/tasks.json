{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Go",
            "type": "shell",
            "command": "go",
            "args": [
                "run",
                "-buildvcs=false",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}